#include <stdio.h>
#include <stdio.h>
#include <string.h>

int main(){
	int num;
	char str[100];
	char reemplazo;

	printf("Introduce la palabra: ");
	scanf("%s", str);

	printf("¿Cuántos caracteres tiene tu palabra? ");
	scanf("%d", &num);

	printf("Introduce la letra que quieres cambiar: ");
	scanf(" %c", &reemplazo);

	str[numero] = reemplazo;

	printf("Tu palabra nueva es: %s\n", str);


	
	return 0;
}
